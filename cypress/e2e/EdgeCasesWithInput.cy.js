describe('Edge cases with complete User Input', () => {
  /*
    Added more test cases to cover further functionalities of the login website. I am used in my manual testing to covering a lot of scenarios.
    beforeEach(() => { 
      cy.visit('https://www.saucedemo.com')
      cy.url().should('include','saucedemo')
    })
    it('misspelt Username', () => {
      cy.get('#user-name').type('standarduser')
      cy.get('#password').type('secret_sauce')
      cy.contains('Login').click()
    })
    it('misspelt Password', () => {
      cy.get('#user-name').type('standard_user')
      cy.get('#password').type('secretsauce')
      cy.contains('Login').click()
    })
    it('Username using incorrect case', () => {
      cy.get('#user-name').type('Standard_user')
      cy.get('#password').type('secret_sauce')
      cy.contains('Login').click()
    })
    it('Password using incorrect case', () => {
      cy.get('#user-name').type('standard_user')
      cy.get('#password').type('Secret_sauce')
      cy.contains('Login').click()
    })
    afterEach(() => {
      cy.get('[data-test="error"]').should('exist')
      cy.contains("Username and password do not match")
    })*/
  })